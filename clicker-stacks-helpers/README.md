# clicker-stacks-helpers

Helper utilities for [`clicker-stacks-sdk`](https://www.npmjs.com/package/clicker-stacks-sdk).

## Usage
```js
const wrapper = require('clicker-stacks-helpers');
console.log(wrapper.describe());
```
